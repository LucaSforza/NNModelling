[project]
name = "nnm-vae-mnist-example"
version = "0.1.0"
description = "Standalone consumer for an NNModelling VAE wheel"
requires-python = ">=3.12"
dependencies = [
  "nnm-vae-mnist",
  "Pillow>=10",
  "torch>=2.0",
  "torchvision>=0.15",
]

[tool.uv]
package = false

[tool.uv.sources]
nnm-vae-mnist = { path = "nnm_vae_mnist-0.1.0-py3-none-any.whl" }

[dependency-groups]
dev = ["pytest>=8"]
